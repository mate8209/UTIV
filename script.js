
const slides=[...document.querySelectorAll('.slide')];const progress=document.querySelector('.progress');const counter=document.querySelector('.slide-counter');const notesBox=document.querySelector('.notes-box');const toc=document.querySelector('.toc');function activeIndex(){let idx=0;let min=1e9;slides.forEach((s,i)=>{const d=Math.abs(s.getBoundingClientRect().top-72);if(d<min){min=d;idx=i}});return idx}function update(){const idx=activeIndex();progress.style.width=((idx+1)/slides.length*100)+'%';counter.textContent=(idx+1)+' / '+slides.length;notesBox.querySelector('p').textContent=slides[idx].dataset.notes||'Sin notas docentes adicionales.'}function go(delta){const idx=Math.max(0,Math.min(slides.length-1,activeIndex()+delta));slides[idx].scrollIntoView({behavior:'smooth'})}window.addEventListener('scroll',update,{passive:true});window.addEventListener('resize',update);document.addEventListener('keydown',e=>{if(e.key==='ArrowRight'||e.key==='PageDown')go(1);if(e.key==='ArrowLeft'||e.key==='PageUp')go(-1);if(e.key.toLowerCase()==='n')notesBox.classList.toggle('open');if(e.key.toLowerCase()==='t')toc.classList.toggle('open')});document.querySelector('[data-prev]').onclick=()=>go(-1);document.querySelector('[data-next]').onclick=()=>go(1);document.querySelector('[data-notes]').onclick=()=>notesBox.classList.toggle('open');document.querySelector('[data-toc]').onclick=()=>toc.classList.toggle('open');toc.addEventListener('click',e=>{if(e.target.closest('a'))toc.classList.remove('open')});update();
